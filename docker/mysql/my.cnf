[mysqld]
character-set-server  = utf8mb4
collation-server      = utf8mb4_unicode_ci
default-time-zone     = +00:00

# Allow larger packets for blob/text testing
max_allowed_packet    = 64M

[client]
default-character-set = utf8mb4

